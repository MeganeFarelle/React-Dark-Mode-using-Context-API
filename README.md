# React-Dark-Mode-using-Context-API
A simple React application demonstrating global Dark Mode management using the Context API. Users can toggle between light and dark themes using a single button, and the theme state is shared across the entire app without prop drilling.
